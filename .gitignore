# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.next/
.next-dev/
out/
build/

# Turborepo
.turbo/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
.dev-logs/

# Testing / coverage
coverage/

# Editor / OS
.DS_Store
.idea/
*.swp
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# TypeScript
*.tsbuildinfo

# Python (services/telegram-agent)
__pycache__/
*.pyc

# Next.js generated types
next-env.d.ts

# Local brand source drop (copies live in apps/web/public/brand)
logo/
.vercel

# Redis dump written by the native redis-server dev.sh starts.
dump.rdb

# Written by `deno check`, which is only ever run by hand against the edge
# functions. It pins nothing the deploy uses, so it is noise in the tree.
deno.lock
.env*
