/**
 * Nothing, deliberately.
 *
 * This held two invented libraries: four music tracks called Sunrise Run,
 * Momentum, Golden Hour and City Lights, and four stock shots called Product
 * macro, Lifestyle wide, Hands detail and Logo sting. There was no audio and no
 * footage behind any of them. They were kept behind a flag so the panels stayed
 * hidden, which was the right call at the time, but a flag guarding fake data is
 * still fake data waiting for somebody to flip it.
 *
 * The panels they fed now say plainly what the editor can and cannot do, so the
 * data has nowhere left to be needed. Real libraries arrive as real files with
 * real URLs, licensed, in storage; when that happens this is where they go and
 * the panels are the surface to render them.
 *
 * The file itself remains because deleting it would leave the story of why in a
 * commit message nobody reads.
 */

export {};
